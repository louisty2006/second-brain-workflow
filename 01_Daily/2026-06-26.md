# 2026-06-26

## Today's Focus
- 完成 TradingAgents MVP v1.0：修穩定性、選對 LLM、成功 E2E、出版到 GitHub。

## Progress
- **MVP v1.0 已 push 上 GitHub**：公開 repo `louisty2006/SU28-Trade-agent`，tag `mvp-v1.0`。
- SSL 修復：新增 `http_utils.py`（certifi），解決 macOS 抓 reddit/stocktwits 的 `CERTIFICATE_VERIFY_FAILED`。
- 混合 LLM 架構：支援 deep/quick 各自 provider/backend_url。
- 辯論輪數改 1；Sentiment 加 freetext-first 加速；app 加每步預計/實際時間 + ETA。
- **LLM 選型定案**：OpenRouter 免費 model 撞 429（tool-call 階段必掛）→ 改用全 Poe：quick=`gpt-4o-mini`、deep=`claude-haiku-4.5`。零 429。
- 成功 E2E：LYFT 長線分析 **192s** 跑完 7/7 步，約 $0.05–0.15/檔。
- 文件：寫 MVP_v1.0.md + 重寫精簡 README（model 選擇表 + token/成本預測）。
- 安全確認：真實 API key 未被 commit，repo 無個人資料洩漏。

## Blockers
- 無重大阻塞（昨日的 Poe 連線波動已透過 retry + freetext-first + haiku/4o-mini 緩解）。
- FRED_API_KEY 未設 → 長線 macro 數據會 skip（非必要）。

## Raw Thoughts
- 教訓：免費 LLM（OpenRouter `:free`）做 tool-calling agent 不可行，429 + 失敗也計配額。一條龍 Poe + 平價 model 才是 MVP 正解。
- 成本驚喜：一檔幾美仙，比之前 Sonnet/Opus 慳 10–300 倍。
